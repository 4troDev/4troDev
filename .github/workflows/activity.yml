name: 🛠 Recent GitHub Activity

on:
  schedule:
    - cron: '0 * * * *' # every hour
  workflow_dispatch:

jobs:
  update-activity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Recent GitHub Activity - Profile Readme
        uses: Readme-Workflows/recent-activity@v2.4.1
        with:
          GH_USERNAME: 4troDev
          COMMIT_MSG: '⚡️ Update recent GitHub activity'
          MAX_LINES: 5
