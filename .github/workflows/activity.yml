name: 🛠 Recent GitHub Activity

on:
  schedule:
    - cron: '0 * * * *'  # every hour
  workflow_dispatch:

jobs:
  update-activity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Recent Activity Readme
        uses: Readme-Workflows/recent-activity@v2.5.0
        with:
          GH_USERNAME: 4troDev
          MAX_LINES: 5
          COMMIT_MSG: '⚡️ Update recent GitHub activity'
